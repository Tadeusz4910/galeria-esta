import ComingSoon from '@/components/ComingSoon'

export const revalidate = 0

// TODO (nastepny krok): tresc strony — adres, mapa dojazdu, godziny otwarcia,
// e-mail, telefon. Na razie placeholder "Wkrotce".
export default function KontaktPage() {
  return <ComingSoon title="Kontakt" active="kontakt" />
}
